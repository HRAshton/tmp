---
- name: Bootstrap Ansible access on build hosts
  hosts: bamboo_build_hosts
  remote_user: root
  gather_facts: false
  become: false
  serial: "{{ deployment_serial | default(1) }}"

  roles:
    - role: bootstrap

